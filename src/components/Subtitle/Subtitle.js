import React from 'react';

// Prop cat => from App.js setting subtitle from each catagories/pages
function Subtitle({cat}) {
  return (
    <h2>I don't know what I want {cat}</h2>
  );
}

export default Subtitle;
